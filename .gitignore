# Rust / Cargo
/target/
**/target/

# Note: We intentionally do NOT ignore `Cargo.lock` for this PoC/app repo,
# so builds are reproducible across machines.

# Local demo data (VDR store, VC, challenge, receipts)
/data/

# IDE / OS
.DS_Store
.vscode/
.idea/

